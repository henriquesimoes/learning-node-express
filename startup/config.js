'use strict';

// const config = require('config');

module.exports = () => {
  // Used for finishing process when a mandatory configuration
  // is not set

  // [
  // ].forEach((name) => {
  //   if (!config.get(name)) {
  //     throw new Error(`${name} is not defined...`);
  //   }
  // });
};
